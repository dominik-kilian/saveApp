<p style="color: green"><%= notice %></p>

<%= render @item %>


  <%= link_to "Edit this save", edit_item_path(@item), class: 'btn btn-info mb-2' %> |
  <%= link_to "Back to saves", items_path, class: 'btn btn-secondary mb-2'%>


<%= button_to "Destroy", @item, method: :delete, class: 'btn btn-danger'%>
